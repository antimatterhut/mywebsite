<section class="hero" id="home">
  <div class="hero-inner container">
    <div class="badges">
      <span class="badge">Available for hire</span>
      <span class="badge">Based in San Diego, CA</span>
    </div>

    <h1>
      Hi, I'm Blake.<br />
      <span class="subhead">Entry-Level IT Professional.</span>
    </h1>

    <p>
      <strong>CompTIA Network+</strong> and <strong>Security+</strong> certified.
      Building skills in <strong>networking</strong>, systems administration,
      and infrastructure.
    </p>

    <div class="cta-group">
      <a href="/projects" class="btn-primary">
        View Projects
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M7 17L17 7M17 7H7M17 7V17" />
        </svg>
      </a>
      <button class="btn-secondary" id="copy-email">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <rect x="9" y="9" width="13" height="13" rx="2" />
          <path d="M5 15H4a2 2 0 01-2-2V4a2 2 0 012-2h9a2 2 0 012 2v1" />
        </svg>
        Copy Email
      </button>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: calc(100vh - 8rem);
    display: flex;
    flex-direction: column;
    justify-content: center;
    font-family: system-ui, -apple-system, sans-serif;
  }

  /* .hero-inner no longer needs its own rules — .container handles width/centering/padding */

  .badges {
    display: flex;
    gap: 0.75rem;
    margin-bottom: 1.5rem;
  }

  .badge {
    border: 1px solid #333333;
    border-radius: 999px;
    padding: 0.4rem 1rem;
    font-size: 0.85rem;
    color: #cccccc;
  }

  h1 {
    font-size: 4rem;
    font-weight: 800;
    line-height: 1.1;
    margin: 0 0 1.5rem 0;
    color: #ffffff;
  }

  .subhead {
    color: #555555;
  }

  p {
    font-size: 1.1rem;
    line-height: 1.6;
    color: #999999;
    max-width: 650px;
    margin-bottom: 2rem;
  }

  p strong {
    color: #cccccc;
    font-weight: 600;
  }

  .cta-group {
    display: flex;
    gap: 1rem;
  }

  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: #ffffff;
    color: #111111;
    text-decoration: none;
    padding: 0.9rem 1.5rem;
    border-radius: 999px;
    font-weight: 600;
    font-size: 0.95rem;
  }

  .btn-secondary {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: #1a1a1a;
    color: #ffffff;
    border: 1px solid #2a2a2a;
    padding: 0.9rem 1.5rem;
    border-radius: 999px;
    font-weight: 600;
    font-size: 0.95rem;
    cursor: pointer;
    font-family: inherit;
  }
</style>

<script>
  document.getElementById('copy-email')?.addEventListener('click', () => {
    navigator.clipboard.writeText('your-email@example.com');
  });
</script>